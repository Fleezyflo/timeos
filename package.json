{
  "name": "moh-time-os-v2",
  "version": "2.0.0",
  "description": "MOH Time OS v2.0 - Modular Google Apps Script task management system",
  "main": "src/",
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "clasp deploy --description \"MOH Time OS v2.0\"",
    "open": "clasp open",
    "logs": "clasp logs",
    "create": "clasp create --type sheets --title \"MOH Time OS v2.0\" --rootDir ./src",
    "build:prod": "node build.js --production",
    "deploy:pre-check": "echo 'Run runPreDeploymentChecks() in Apps Script console'",
    "deploy:backup": "echo 'Run BackupManager.createBackup() in Apps Script console'",
    "deploy:push": "clasp push",
    "deploy:verify": "echo 'Run runPostDeploymentVerification() in Apps Script console'"
  },
  "keywords": ["google-apps-script", "task-management", "scheduling", "productivity"],
  "author": "MOH",
  "license": "MIT",
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}